# Optional in local dev: leave unset to use Vite proxy (/api → http://localhost:5000).
# Required for production builds: full URL of the API (must include /api path prefix).
# VITE_API_BASE_URL=https://your-api-host.example.com/api
